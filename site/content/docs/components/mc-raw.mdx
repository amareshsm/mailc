---
title: mc-raw
description: "Passthrough — content is emitted as-is with no transformation."
---

Passthrough — content is emitted as-is with no transformation.

## Quick facts

| | |
|---|---|
| Category | `content` |
| Allowed parents | `mc-column`, `mc-body`, `mc-section`, `root` |
| Allowed children | _(none)_ |
| Accepts text content | yes |
| Accepts `class` attribute | yes |
| Compiles to |  |

## Required attributes

_(none)_


## Optional attributes

| Attribute | Type | Default | Example | Description |
|---|---|---|---|---|
| `class` | `tailwind-classes` | — | `hidden` | Utility classes. |
| `id` | `string` | — | `raw-block` | Unique identifier. |


## Compiles to

Output elements: .

No transformation — content is written directly to output.

## Example markup

```html
<mc-raw>Passthrough — content is emitted as-is with no transformation</mc-raw>
```

## Programmatic access

```ts
import { introspect } from 'mailc'

const spec = introspect.component('mc-raw')
```

See [`introspect.component(type)`](/docs/api/introspect#introspect-component) for the full `ComponentSpec` shape.
