---
title: mc-all
description: "Sets global default attributes for all components (inside mc-attributes)."
---

Sets global default attributes for all components (inside mc-attributes).

## Quick facts

| | |
|---|---|
| Category | `head` |
| Allowed parents | `mc-attributes` |
| Allowed children | _(none)_ |
| Accepts text content | no |
| Accepts `class` attribute | no |
| Compiles to |  |

## Required attributes

_(none)_


## Optional attributes

| Attribute | Type | Default | Example | Description |
|---|---|---|---|---|
| `font-family` | `string` | — | `Arial, sans-serif` | Global default font family. |
| `font-size` | `css-value` | — | `16px` | Global default font size. |
| `font-weight` | `normal` \| `bold` | — | `normal` | Global default font weight. |
| `color` | `color` | — | `#333333` | Global default text color. |
| `line-height` | `css-value` | — | `1.6` | Global default line height. |
| `letter-spacing` | `css-value` | — | `0` | Global default letter spacing. |
| `padding` | `css-value` | — | `0` | Global default padding. |


## Compiles to

Output elements: .

Compile-time directive — no HTML output.

## Example markup

```html
<mc-all />
```

## Programmatic access

```ts
import { introspect } from 'mailc'

const spec = introspect.component('mc-all')
```

See [`introspect.component(type)`](/docs/api/introspect#introspect-component) for the full `ComponentSpec` shape.
