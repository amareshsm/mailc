---
title: mc-attributes
description: "Defines default attribute values for component types within scope."
---

Defines default attribute values for component types within scope.

## Quick facts

| | |
|---|---|
| Category | `head` |
| Allowed parents | `mc-head` |
| Allowed children | `mc-all`, `mc-class` |
| Accepts text content | no |
| Accepts `class` attribute | yes |
| Compiles to |  |

## Required attributes

_(none)_


## Optional attributes

| Attribute | Type | Default | Example | Description |
|---|---|---|---|---|
| `class` | `tailwind-classes` | — | — | Utility classes. |
| `id` | `string` | — | `attrs` | Unique identifier. |


## Compiles to

Output elements: .

Compile-time directive only — no HTML output.

## Example markup

```html
<mc-attributes></mc-attributes>
```

## Programmatic access

```ts
import { introspect } from 'mailc'

const spec = introspect.component('mc-attributes')
```

See [`introspect.component(type)`](/docs/api/introspect#introspect-component) for the full `ComponentSpec` shape.
